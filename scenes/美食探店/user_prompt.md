请分析以下B站美食探店视频评论数据，执行“店铺推荐挖掘（输出JSON）”任务。

本次任务目标（按优先级）：
1) 找出评论区中被用户推荐安利的具体店铺（包括楼中楼回复）
2) 识别避雷店铺、分歧店铺
3) 过滤低信息量评论（例如仅“推荐XXX”无理由/无体验/无细节，不纳入推荐统计）
4) 输出可直接落盘保存的合法JSON结果（不要输出任何解释文字）

本次统计口径强调：
- mention_count 可以统计提及
- recommend_count / warn_count 只统计 info_score >= 2 且满足“有效推荐/有效避雷”的评论
- 未达到店铺纳入门槛的店铺不要进入 shops，放入 omitted_shops
- 不要把菜名当店名；菜名可以作为 signature_dishes_or_keywords
- 对于简称、别名、谐音梗请尽量归一，但不确定时保持谨慎并降低置信度

如果输入内容过长，请优先保证：
- 店铺识别与有效性过滤准确
- 输出JSON合法
并在 summary.notes 中说明覆盖范围或处理限制。